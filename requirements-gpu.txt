# GPU-этап: детекция, поза, human parsing, VAE-латенты.
# Ставится на машине с CUDA. torch НЕ пинится здесь: колесо зависит от версии
# CUDA — ставится отдельно по инструкции с pytorch.org, см. README.
-r requirements.txt
transformers>=4.47
diffusers>=0.31
accelerate>=1.0
safetensors>=0.4
huggingface-hub>=0.26
# ViTPose режет кроп по боксу аффинным преобразованием через scipy. Без него
# transformers падает не проверкой зависимости, а NameError внутри себя.
scipy>=1.11
# Метрики оценки: SSIM, LPIPS, FID.
torchmetrics[image]>=1.4
